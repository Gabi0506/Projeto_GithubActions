# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python application

on:
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest  # Usando uma máquina virtual com Ubuntu
    
    steps:
      - name: Check out repository
        uses: actions/checkout@v3
        
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'  # Definindo a versão do Python

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt  # Instalando dependências

      - name: Build the application (se necessário)
        run: |
          echo "Build step - Custom build commands here"
          # Se você tiver comandos de build específicos, adicione-os aqui.
          # Exemplo: python setup.py install ou outros comandos relacionados ao seu app
